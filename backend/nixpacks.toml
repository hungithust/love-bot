[phases.setup]
nixPkgs = ["python311", "python311Packages.pip"]

[phases.install]
cmds = ["python3.11 -m venv /app/.venv", "/app/.venv/bin/pip install -e ."]

[start]
cmd = "/app/.venv/bin/uvicorn main:app --host 0.0.0.0 --port $PORT"
