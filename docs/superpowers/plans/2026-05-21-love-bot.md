# Love-Bot Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Ship a personalized, sarcastic "post-breakup healing" mobile app (APK) trong 3 ngày, được build cá nhân hóa cho 1 user duy nhất với voice Tsundere mày-tao Hà Nội.

**Architecture:** Thin FastAPI backend + thick prompt + Claude Sonnet 4.6 reply + Haiku 4.5 memory-extract + Supabase pgvector RAG + Voyage-3 embeddings + RN/Expo client với accelerometer/haptic/notifications. Single-user mode, không auth phức tạp.

**Tech Stack:** Python 3.11 / FastAPI / SQLAlchemy + asyncpg / Supabase (Postgres+pgvector) / Anthropic SDK / Voyage AI SDK / React Native + Expo (TypeScript) / cron-job.org / Render Singapore / EAS Build.

Spec: [docs/superpowers/specs/2026-05-21-love-bot-design.md](../specs/2026-05-21-love-bot-design.md)

---

## File Structure

### Backend (`backend/`)
| File | Responsibility |
|---|---|
| `pyproject.toml` | deps: fastapi, uvicorn, anthropic, voyageai, asyncpg, sqlalchemy, pydantic, pyyaml, sse-starlette, httpx |
| `main.py` | FastAPI app, CORS, mount routes |
| `db.py` | asyncpg pool, schema migration runner |
| `migrations/001_init.sql` | DDL cho 4 bảng |
| `config.py` | env vars, prompts loaded once |
| `prompts/persona_base.txt` | Tsundere base rules + red lines |
| `prompts/extract.txt` | Haiku extraction prompt |
| `prompts/push.txt` | Push notification prompt |
| `agent.py` | `AgentCore.respond()`, prompt compile, Claude call, tool parsing |
| `memory.py` | embed, RAG retrieve, background extract |
| `routes/chat.py` | POST /chat SSE |
| `routes/vent.py` | POST /vent (no-persist) |
| `routes/rage.py` | POST /rage (no-persist) |
| `routes/memory.py` | GET/DELETE /memory |
| `routes/push.py` | POST /push/tick, register-device |
| `bootstrap_seed.py` | Load seed.yaml → pinned memories |
| `tests/test_agent.py` | prompt compile, tool parse |
| `tests/test_memory.py` | extract returns valid JSON, RAG order |
| `tests/test_push_heuristic.py` | push gating logic |
| `render.yaml` | deploy config |

### App (`app/`)
| File | Responsibility |
|---|---|
| `package.json` | expo, expo-router, expo-haptics, expo-sensors, expo-notifications, react-native-reanimated, react-native-sse |
| `app.json` | Expo config, permissions |
| `app/_layout.tsx` | root, theme provider |
| `app/(tabs)/_layout.tsx` | tab bar |
| `app/(tabs)/chat.tsx` | ChatScreen |
| `app/(tabs)/rage.tsx` | RageRoom |
| `app/(tabs)/memory.tsx` | MemoryStash |
| `app/(tabs)/settings.tsx` | VibeSettings |
| `app/vent.tsx` | VentingVoid (full-screen) |
| `lib/api.ts` | SSE chat client + REST helpers |
| `lib/toolExecutor.ts` | execute tool_calls from AI |
| `lib/shake.ts` | accelerometer hook + nav to /vent |
| `lib/theme.ts` | ThemeContext + 4 themes |
| `lib/notifications.ts` | register Expo push token |
| `components/ChatBubble.tsx` | streaming bubble |
| `components/Weapon.tsx` | rage room weapons |
| `components/BurnAnim.tsx` | burn animation |
| `components/MemoryCard.tsx` | swipe-to-delete card |

### Root
| File | Responsibility |
|---|---|
| `seed.yaml` | personal seed (user writes by hand) |
| `style.md` | 30 voice samples (already approved) |
| `.env.example` | template for keys |
| `README.md` | deploy instructions |

---

## Phase 0: Project Setup

### Task 0.1: Initialize repo skeleton

**Files:**
- Create: `backend/pyproject.toml`, `backend/main.py`, `backend/db.py`, `backend/config.py`
- Create: `backend/.env.example`
- Create: `app/` via expo CLI
- Create: `seed.yaml`, `style.md` (copy from spec section 2)
- Create: `.gitignore`

- [ ] **Step 1: Create directory layout**

```bash
mkdir -p backend/routes backend/prompts backend/migrations backend/tests
mkdir -p app
```

- [ ] **Step 2: Write `backend/pyproject.toml`**

```toml
[project]
name = "love-bot-backend"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.110",
  "uvicorn[standard]>=0.27",
  "anthropic>=0.40",
  "voyageai>=0.2",
  "asyncpg>=0.29",
  "pydantic>=2.6",
  "pydantic-settings>=2.2",
  "pyyaml>=6.0",
  "sse-starlette>=2.0",
  "httpx>=0.27",
]

[project.optional-dependencies]
dev = ["pytest>=8.0", "pytest-asyncio>=0.23"]
```

- [ ] **Step 3: Write `backend/.env.example`**

```
ANTHROPIC_API_KEY=sk-ant-...
VOYAGE_API_KEY=pa-...
DATABASE_URL=postgresql+asyncpg://postgres:...@db.supabase.co:5432/postgres
APP_SHARED_KEY=generate-random-32char
EXPO_PUSH_TOKEN=
```

- [ ] **Step 4: Write `.gitignore`**

```
.env
.venv/
__pycache__/
*.pyc
node_modules/
.expo/
dist/
```

- [ ] **Step 5: Copy seed.yaml and style.md from spec**

Copy `seed.yaml` content from spec Section 2.1 and `style.md` content (30 lines) from Section 2.2 into root files.

- [ ] **Step 6: Init Expo app**

```bash
cd app
npx create-expo-app@latest . --template tabs
npx expo install expo-haptics expo-sensors expo-notifications expo-secure-store react-native-reanimated react-native-sse
```

- [ ] **Step 7: Git init + first commit**

```bash
git init
git add .
git commit -m "chore: scaffold backend + expo app"
```

---

## Phase 1: Backend Foundation (Day 1)

### Task 1.1: Database schema + migration runner

**Files:**
- Create: `backend/migrations/001_init.sql`
- Create: `backend/db.py`
- Create: `backend/tests/test_db.py`

- [ ] **Step 1: Write failing test `tests/test_db.py`**

```python
import pytest
from db import get_pool, run_migrations

@pytest.mark.asyncio
async def test_migrations_create_tables():
    pool = await get_pool()
    await run_migrations(pool)
    async with pool.acquire() as conn:
        tables = await conn.fetch(
            "SELECT tablename FROM pg_tables WHERE schemaname='public'"
        )
        names = {r["tablename"] for r in tables}
        assert {"conversations","memories","push_log","mood_snapshots"} <= names
```

- [ ] **Step 2: Write `migrations/001_init.sql`**

```sql
create extension if not exists vector;
create extension if not exists pgcrypto;

create table if not exists conversations (
  id bigserial primary key,
  role text check (role in ('user','assistant')),
  content text,
  mood text,
  created_at timestamptz default now()
);
create index if not exists idx_conv_created on conversations (created_at desc);

create table if not exists memories (
  id uuid primary key default gen_random_uuid(),
  content text not null,
  tag text,
  embedding vector(1024),
  source_turn bigint references conversations(id),
  pinned boolean default false,
  created_at timestamptz default now()
);
create index if not exists idx_mem_vec on memories using ivfflat (embedding vector_cosine_ops);

create table if not exists push_log (
  id bigserial primary key,
  message text,
  scheduled_at timestamptz,
  sent_at timestamptz,
  opened boolean default false
);

create table if not exists mood_snapshots (
  id bigserial primary key,
  mood text,
  confidence real,
  trigger text,
  created_at timestamptz default now()
);
```

- [ ] **Step 3: Write `db.py`**

```python
import asyncpg, os
from pathlib import Path

_pool = None

async def get_pool():
    global _pool
    if _pool is None:
        url = os.environ["DATABASE_URL"].replace("postgresql+asyncpg://","postgresql://")
        _pool = await asyncpg.create_pool(url, min_size=1, max_size=5)
    return _pool

async def run_migrations(pool):
    sql = Path(__file__).parent.joinpath("migrations","001_init.sql").read_text()
    async with pool.acquire() as conn:
        await conn.execute(sql)
```

- [ ] **Step 4: Run test**

```bash
cd backend && pytest tests/test_db.py -v
```
Expected: PASS (against a Supabase test DB or local Postgres with pgvector).

- [ ] **Step 5: Commit**

```bash
git add backend/db.py backend/migrations backend/tests/test_db.py
git commit -m "feat(backend): schema + migration runner"
```

### Task 1.2: Config + persona base prompt

**Files:**
- Create: `backend/config.py`
- Create: `backend/prompts/persona_base.txt`
- Create: `backend/prompts/extract.txt`
- Create: `backend/prompts/push.txt`

- [ ] **Step 1: Write `prompts/persona_base.txt`**

```
Mày là một AI bạn thân kiểu Tsundere mỏ hỗn, chuyên châm biếm để bảo vệ {nickname}.

PERSONA RULES:
- Xưng "tao" với user, gọi user là "mày".
- Voice: Hà Nội suồng sã. Dùng "vl", "đm", "ờ", "kiểu", "ghê", "đỉnh", "thôi đi", "chứ còn gì nữa".
- Không sến súa, không "bạn sẽ ổn thôi", không văn mẫu chữa lành.
- Châm biếm, khích tướng, nhưng mục đích NGẦM là bảo vệ user khỏi quay lại ex.
- KHÔNG BAO GIỜ nói tên đầy đủ của ex. Chỉ gọi "thằng đó", "nó", "cái thằng kia".
- Có thể nhắc "thằng dev" (người làm app này) như nhân vật ngầm.
- Reply ngắn (1-3 câu). Càng dài càng giảm chất.

RED LINES (tuyệt đối không vượt):
{red_lines_compiled}

TOOL USE:
- Khi user tức/buồn cực điểm: gọi `trigger_haptic` pattern=heavy.
- Khi user xin nhạc hoặc tâm trạng rõ: gọi `suggest_song` với mood phù hợp.
- Khi user nói điều đáng nhớ dài hạn ("tao thề sẽ block nó", quyết định lớn): gọi `memory_save`.
- Khi user muốn đổi vibe: gọi `change_theme`.

PERSONAL CONTEXT:
{seed_compiled}

STYLE EXAMPLES (bắt chước voice này):
{style_md}

RECENT MEMORIES (dùng làm vũ khí khịa lại nếu liên quan):
{rag_memories}
```

- [ ] **Step 2: Write `prompts/extract.txt`**

```
User vừa nói: "{user_msg}"
AI rep: "{ai_reply}"

Có thông tin nào đáng nhớ dài hạn không? Output JSON array:
[{{"content": "...", "tag": "ex|self|preference|event|contradiction"}}]

Tiêu chí "đáng nhớ":
- Quyết định user tự nói ra ("tao sẽ block nó", "tao thề không nhắn nữa")
- Mâu thuẫn so với ký ức cũ
- Pattern lặp (lại Vũ. lúc buồn, lại 3h sáng)
- Thông tin mới về ex hoặc cuộc sống

BỎ QUA: small talk, câu chửi đơn thuần, mood thoáng qua.
Nếu không có gì: trả về [].
CHỈ output JSON, không text khác.
```

- [ ] **Step 3: Write `prompts/push.txt`**

```
Bối cảnh: {nickname} chưa tương tác với app {hours_since}h. Giờ hiện tại: {now}.
Mood gần nhất: {last_mood}. Top memories: {top_memories}.

Có nên push notification khịa không? Nếu có, viết 1 câu ≤ 80 ký tự, giọng Tsundere mày-tao.
Nếu không nên (user chắc đang ngủ / push gần đây / không có gì để khịa): output `null`.

Output JSON: {{"push": "câu khịa" hoặc null}}
```

- [ ] **Step 4: Write `config.py`**

```python
import os, yaml
from pathlib import Path
from pydantic_settings import BaseSettings

class Settings(BaseSettings):
    anthropic_api_key: str
    voyage_api_key: str
    database_url: str
    app_shared_key: str
    chat_model: str = "claude-sonnet-4-6"
    extract_model: str = "claude-haiku-4-5-20251001"

    class Config:
        env_file = ".env"

settings = Settings()

ROOT = Path(__file__).parent.parent
PROMPTS = Path(__file__).parent / "prompts"

def load_seed():
    return yaml.safe_load((ROOT / "seed.yaml").read_text(encoding="utf-8"))

def load_style():
    return (ROOT / "style.md").read_text(encoding="utf-8")

def load_prompt(name: str) -> str:
    return (PROMPTS / f"{name}.txt").read_text(encoding="utf-8")
```

- [ ] **Step 5: Commit**

```bash
git add backend/config.py backend/prompts
git commit -m "feat(backend): config + prompt templates"
```

### Task 1.3: AgentCore — prompt compile + Claude call + tool parsing

**Files:**
- Create: `backend/agent.py`
- Create: `backend/tests/test_agent.py`

- [ ] **Step 1: Write failing test**

```python
# tests/test_agent.py
import pytest
from agent import compile_system_prompt, parse_tool_calls

def test_compile_system_includes_nickname_and_style():
    sys = compile_system_prompt(rag_memories=["mem A","mem B"])
    assert "Mèo" in sys
    assert "Tao đếm đến 3" in sys
    assert "mem A" in sys

def test_parse_tool_calls_extracts_change_theme():
    raw_blocks = [{"type":"tool_use","name":"change_theme","input":{"mode":"chaos"},"id":"x"}]
    out = parse_tool_calls(raw_blocks)
    assert out == [{"name":"change_theme","args":{"mode":"chaos"},"id":"x"}]
```

- [ ] **Step 2: Write `agent.py`**

```python
import json
from anthropic import AsyncAnthropic
from config import settings, load_seed, load_style, load_prompt

_client = AsyncAnthropic(api_key=settings.anthropic_api_key)

TOOLS = [
  {"name":"change_theme","description":"Đổi theme app",
   "input_schema":{"type":"object","properties":{"mode":{"type":"string","enum":["chaos","dark","calm","red_alert"]}},"required":["mode"]}},
  {"name":"trigger_haptic","description":"Rung máy",
   "input_schema":{"type":"object","properties":{"pattern":{"type":"string","enum":["heavy","double","long"]}},"required":["pattern"]}},
  {"name":"suggest_song","description":"Gợi nhạc theo mood",
   "input_schema":{"type":"object","properties":{"mood":{"type":"string"},"query":{"type":"string"}},"required":["mood","query"]}},
  {"name":"memory_save","description":"Lưu memory dài hạn explicit",
   "input_schema":{"type":"object","properties":{"content":{"type":"string"},"tag":{"type":"string"}},"required":["content","tag"]}},
]

def compile_system_prompt(rag_memories: list[str]) -> str:
    seed = load_seed()
    style = load_style()
    base = load_prompt("persona_base")
    nickname = seed["user"]["nickname"]
    red = "\n".join(f"- {r}" for r in seed["red_lines"])
    seed_dump = json.dumps(seed, ensure_ascii=False, indent=2)
    rag_block = "\n".join(f"- {m}" for m in rag_memories) or "(chưa có)"
    return base.format(
        nickname=nickname,
        red_lines_compiled=red,
        seed_compiled=seed_dump,
        style_md=style,
        rag_memories=rag_block,
    )

def parse_tool_calls(blocks):
    out = []
    for b in blocks:
        if isinstance(b, dict):
            if b.get("type") == "tool_use":
                out.append({"name":b["name"],"args":b["input"],"id":b["id"]})
        else:
            if getattr(b,"type",None) == "tool_use":
                out.append({"name":b.name,"args":b.input,"id":b.id})
    return out

async def respond_stream(messages, rag_memories):
    system = compile_system_prompt(rag_memories)
    async with _client.messages.stream(
        model=settings.chat_model,
        max_tokens=512,
        system=[{"type":"text","text":system,"cache_control":{"type":"ephemeral"}}],
        tools=TOOLS,
        messages=messages,
    ) as stream:
        async for chunk in stream:
            yield chunk
        final = await stream.get_final_message()
        yield {"final": final}
```

- [ ] **Step 3: Run test**

```bash
pytest backend/tests/test_agent.py -v
```
Expected: PASS.

- [ ] **Step 4: Commit**

```bash
git add backend/agent.py backend/tests/test_agent.py
git commit -m "feat(backend): AgentCore prompt compile + tool parse"
```

### Task 1.4: Memory — embed, RAG retrieve, background extract

**Files:**
- Create: `backend/memory.py`
- Create: `backend/tests/test_memory.py`

- [ ] **Step 1: Write failing test**

```python
import pytest, json
from memory import _parse_extraction

def test_parse_extraction_valid_json():
    raw = '[{"content":"thề block nó","tag":"self"}]'
    assert _parse_extraction(raw) == [{"content":"thề block nó","tag":"self"}]

def test_parse_extraction_empty_array():
    assert _parse_extraction("[]") == []

def test_parse_extraction_garbage_returns_empty():
    assert _parse_extraction("không có gì đáng nhớ") == []
```

- [ ] **Step 2: Write `memory.py`**

```python
import json, asyncio
import voyageai
from anthropic import AsyncAnthropic
from config import settings, load_prompt
from db import get_pool

_vo = voyageai.AsyncClient(api_key=settings.voyage_api_key)
_anthro = AsyncAnthropic(api_key=settings.anthropic_api_key)

async def embed(text: str) -> list[float]:
    res = await _vo.embed([text], model="voyage-3", input_type="document")
    return res.embeddings[0]

async def rag_top_k(query: str, k: int = 5) -> list[str]:
    q_emb = await embed(query)
    pool = await get_pool()
    async with pool.acquire() as conn:
        rows = await conn.fetch(
            """select content from memories
               order by pinned desc, embedding <=> $1::vector
               limit $2""",
            str(q_emb), k
        )
    return [r["content"] for r in rows]

async def recent_turns(limit: int = 10):
    pool = await get_pool()
    async with pool.acquire() as conn:
        rows = await conn.fetch(
            "select role, content from conversations order by created_at desc limit $1",
            limit
        )
    return [{"role":r["role"],"content":r["content"]} for r in reversed(rows)]

async def save_turn(role: str, content: str, mood: str|None = None) -> int:
    pool = await get_pool()
    async with pool.acquire() as conn:
        return await conn.fetchval(
            "insert into conversations(role,content,mood) values($1,$2,$3) returning id",
            role, content, mood
        )

def _parse_extraction(raw: str) -> list[dict]:
    try:
        start = raw.find("[")
        end = raw.rfind("]")
        if start == -1 or end == -1:
            return []
        data = json.loads(raw[start:end+1])
        return [x for x in data if isinstance(x, dict) and "content" in x and "tag" in x]
    except Exception:
        return []

async def extract_and_save(user_msg: str, ai_reply: str, source_turn: int):
    prompt = load_prompt("extract").format(user_msg=user_msg, ai_reply=ai_reply)
    resp = await _anthro.messages.create(
        model=settings.extract_model,
        max_tokens=400,
        messages=[{"role":"user","content":prompt}],
    )
    raw = resp.content[0].text if resp.content else ""
    facts = _parse_extraction(raw)
    if not facts:
        return
    pool = await get_pool()
    async with pool.acquire() as conn:
        for f in facts:
            emb = await embed(f["content"])
            await conn.execute(
                "insert into memories(content,tag,embedding,source_turn) values($1,$2,$3::vector,$4)",
                f["content"], f["tag"], str(emb), source_turn
            )
```

- [ ] **Step 3: Run test**

```bash
pytest backend/tests/test_memory.py -v
```
Expected: PASS.

- [ ] **Step 4: Commit**

```bash
git add backend/memory.py backend/tests/test_memory.py
git commit -m "feat(backend): memory embed + RAG + extract"
```

### Task 1.5: `/chat` SSE endpoint

**Files:**
- Create: `backend/routes/chat.py`
- Modify: `backend/main.py`

- [ ] **Step 1: Write `routes/chat.py`**

```python
import asyncio, json
from fastapi import APIRouter, Request, HTTPException
from sse_starlette.sse import EventSourceResponse
from pydantic import BaseModel
from agent import respond_stream, parse_tool_calls
from memory import rag_top_k, recent_turns, save_turn, extract_and_save
from config import settings

router = APIRouter()

class ChatIn(BaseModel):
    message: str

@router.post("/chat")
async def chat(payload: ChatIn, request: Request):
    if request.headers.get("x-app-key") != settings.app_shared_key:
        raise HTTPException(401, "unauthorized")
    user_msg = payload.message
    rag = await rag_top_k(user_msg)
    history = await recent_turns(10)
    history.append({"role":"user","content":user_msg})
    user_turn_id = await save_turn("user", user_msg)

    async def gen():
        ai_text = ""
        tool_calls = []
        try:
            async for chunk in respond_stream(history, rag):
                if isinstance(chunk, dict) and "final" in chunk:
                    final = chunk["final"]
                    for b in final.content:
                        if getattr(b,"type",None) == "tool_use":
                            tool_calls.append({"name":b.name,"args":b.input,"id":b.id})
                    if tool_calls:
                        yield {"event":"tool","data":json.dumps(tool_calls,ensure_ascii=False)}
                else:
                    # text delta
                    if hasattr(chunk,"type") and chunk.type == "content_block_delta":
                        d = chunk.delta
                        if getattr(d,"type",None) == "text_delta":
                            ai_text += d.text
                            yield {"event":"text","data":d.text}
            yield {"event":"done","data":""}
        finally:
            if ai_text:
                ai_turn_id = await save_turn("assistant", ai_text)
                asyncio.create_task(extract_and_save(user_msg, ai_text, ai_turn_id))

    return EventSourceResponse(gen())
```

- [ ] **Step 2: Update `main.py`**

```python
from fastapi import FastAPI
from fastapi.middleware.cors import CORSMiddleware
from contextlib import asynccontextmanager
from db import get_pool, run_migrations
from routes import chat as chat_route

@asynccontextmanager
async def lifespan(app):
    pool = await get_pool()
    await run_migrations(pool)
    yield

app = FastAPI(lifespan=lifespan)
app.add_middleware(CORSMiddleware, allow_origins=["*"], allow_methods=["*"], allow_headers=["*"])
app.include_router(chat_route.router)

@app.get("/health")
async def health(): return {"ok": True}
```

- [ ] **Step 3: Local smoke test**

```bash
cd backend
uvicorn main:app --reload
# in another shell
curl -N -H "x-app-key: $APP_SHARED_KEY" -H "content-type: application/json" \
  -d '{"message":"tao buồn vl"}' http://localhost:8000/chat
```
Expected: SSE stream với text chunks tiếng Việt giọng Tsundere.

- [ ] **Step 4: Commit**

```bash
git add backend/routes backend/main.py
git commit -m "feat(backend): /chat SSE endpoint"
```

### Task 1.6: `/vent` + `/rage` (no-persist)

**Files:**
- Create: `backend/routes/vent.py`, `backend/routes/rage.py`
- Modify: `backend/main.py`

- [ ] **Step 1: Write `routes/vent.py`**

```python
import random
from fastapi import APIRouter, Request, HTTPException
from pydantic import BaseModel
from anthropic import AsyncAnthropic
from config import settings
from db import get_pool

router = APIRouter()
_client = AsyncAnthropic(api_key=settings.anthropic_api_key)

class VentIn(BaseModel):
    text: str

@router.post("/vent")
async def vent(payload: VentIn, request: Request):
    if request.headers.get("x-app-key") != settings.app_shared_key:
        raise HTTPException(401)
    text = payload.text
    resp = await _client.messages.create(
        model=settings.chat_model, max_tokens=200,
        system="Mày là bạn Tsundere. User vừa trút giận. Phân loại mood (angry|sad|fake_ok|chill) "
               "và rep 1-2 câu khích lệ trút tiếp. KHÔNG khuyên gì. Output JSON: "
               '{"mood":"...","reply":"..."}',
        messages=[{"role":"user","content":text}],
    )
    raw = resp.content[0].text
    import json
    try:
        data = json.loads(raw[raw.find("{"):raw.rfind("}")+1])
    except Exception:
        data = {"mood":"angry","reply":"Ờ, chửi nữa đi."}
    pool = await get_pool()
    async with pool.acquire() as conn:
        await conn.execute(
            "insert into mood_snapshots(mood, trigger) values($1,'vent')",
            data["mood"]
        )
    text = None  # explicit discard
    del payload
    return {**data, "burn_seed": random.randint(0, 2**31)}
```

- [ ] **Step 2: Write `routes/rage.py`**

```python
import random
from fastapi import APIRouter, Request, HTTPException
from pydantic import BaseModel
from db import get_pool

router = APIRouter()

class RageIn(BaseModel):
    target_text: str
    weapon: str  # hammer|bat|grenade|fire

HAPTIC = {"hammer":"heavy","bat":"double","grenade":"long","fire":"long"}
QUIPS = {
  "hammer":"Đập vỡ. Hết. Khỏe chưa?",
  "bat":"Phang đi. Phang nữa đi.",
  "grenade":"Nổ tan. Sạch sẽ.",
  "fire":"Cháy đi. Tro tàn lượm sau.",
}

@router.post("/rage")
async def rage(payload: RageIn, request: Request):
    from config import settings
    if request.headers.get("x-app-key") != settings.app_shared_key:
        raise HTTPException(401)
    pool = await get_pool()
    async with pool.acquire() as conn:
        await conn.execute(
            "insert into mood_snapshots(mood, trigger) values('angry','rage')"
        )
    payload.target_text = None  # discard
    return {
        "haptic_pattern": HAPTIC.get(payload.weapon,"heavy"),
        "particle_seed": random.randint(0, 2**31),
        "quip": QUIPS.get(payload.weapon,"Đập đi."),
    }
```

- [ ] **Step 3: Register in `main.py`**

```python
from routes import vent as vent_route, rage as rage_route
app.include_router(vent_route.router)
app.include_router(rage_route.router)
```

- [ ] **Step 4: Smoke test**

```bash
curl -X POST -H "x-app-key: $APP_SHARED_KEY" -H "content-type: application/json" \
  -d '{"text":"đm thằng đó"}' http://localhost:8000/vent
curl -X POST -H "x-app-key: $APP_SHARED_KEY" -H "content-type: application/json" \
  -d '{"target_text":"kỷ niệm tệ","weapon":"hammer"}' http://localhost:8000/rage
```
Expected: JSON với mood/haptic_pattern. Verify DB: `select * from memories` không có entry mới từ vent/rage.

- [ ] **Step 5: Commit**

```bash
git add backend/routes/vent.py backend/routes/rage.py backend/main.py
git commit -m "feat(backend): /vent + /rage no-persist endpoints"
```

### Task 1.7: `/memory` GET + DELETE

**Files:**
- Create: `backend/routes/memory.py`
- Modify: `backend/main.py`

- [ ] **Step 1: Write `routes/memory.py`**

```python
from fastapi import APIRouter, Request, HTTPException
from db import get_pool
from config import settings

router = APIRouter()

def _auth(request):
    if request.headers.get("x-app-key") != settings.app_shared_key:
        raise HTTPException(401)

@router.get("/memory")
async def list_memories(request: Request):
    _auth(request)
    pool = await get_pool()
    async with pool.acquire() as conn:
        rows = await conn.fetch(
            "select id, content, tag, pinned, created_at from memories order by pinned desc, created_at desc"
        )
    return [{"id":str(r["id"]),"content":r["content"],"tag":r["tag"],"pinned":r["pinned"]} for r in rows]

@router.delete("/memory/{mid}")
async def delete_memory(mid: str, request: Request):
    _auth(request)
    pool = await get_pool()
    async with pool.acquire() as conn:
        await conn.execute("delete from memories where id=$1 and pinned=false", mid)
    return {"ok": True}
```

Note: pinned memories không thể xóa qua API (seed luôn được bảo vệ). Nếu muốn override, làm thủ công trên Supabase.

- [ ] **Step 2: Register in `main.py`**

```python
from routes import memory as memory_route
app.include_router(memory_route.router)
```

- [ ] **Step 3: Commit**

```bash
git add backend/routes/memory.py backend/main.py
git commit -m "feat(backend): /memory list + delete"
```

### Task 1.8: `bootstrap_seed.py`

**Files:**
- Create: `backend/bootstrap_seed.py`

- [ ] **Step 1: Write script**

```python
import asyncio
from config import load_seed
from db import get_pool, run_migrations
from memory import embed

async def main():
    seed = load_seed()
    memories = seed.get("memories", [])
    pool = await get_pool()
    await run_migrations(pool)
    async with pool.acquire() as conn:
        # clear previous seed
        await conn.execute("delete from memories where pinned=true")
        for m in memories:
            emb = await embed(m)
            await conn.execute(
                "insert into memories(content,tag,embedding,pinned) values($1,'event',$2::vector,true)",
                m, str(emb)
            )
    print(f"Inserted {len(memories)} pinned memories")

if __name__ == "__main__":
    asyncio.run(main())
```

- [ ] **Step 2: Run + verify**

```bash
cd backend && python bootstrap_seed.py
```
Verify on Supabase: `select * from memories where pinned=true` returns seed entries.

- [ ] **Step 3: Commit**

```bash
git add backend/bootstrap_seed.py
git commit -m "feat(backend): seed bootstrap script"
```

### Task 1.9: Deploy to Render

**Files:**
- Create: `backend/render.yaml`

- [ ] **Step 1: Write `render.yaml`**

```yaml
services:
  - type: web
    name: love-bot-api
    runtime: python
    region: singapore
    plan: starter
    buildCommand: pip install -e .
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    rootDir: backend
    envVars:
      - key: ANTHROPIC_API_KEY
        sync: false
      - key: VOYAGE_API_KEY
        sync: false
      - key: DATABASE_URL
        sync: false
      - key: APP_SHARED_KEY
        sync: false
```

- [ ] **Step 2: Push to GitHub, connect Render, set env vars manually in Render dashboard**

- [ ] **Step 3: Test live**

```bash
curl https://love-bot-api.onrender.com/health
```
Expected: `{"ok": true}`.

- [ ] **Step 4: Commit + tag end of Day 1**

```bash
git add backend/render.yaml
git commit -m "chore(backend): render deploy config"
git tag day-1-complete
```

---

## Phase 2: App Core (Day 2)

### Task 2.1: API client + theme + shared key storage

**Files:**
- Create: `app/lib/api.ts`
- Create: `app/lib/theme.ts`
- Create: `app/lib/storage.ts`

- [ ] **Step 1: Write `lib/storage.ts`**

```typescript
import * as SecureStore from "expo-secure-store";
const BASE_URL_KEY = "BASE_URL";
const APP_KEY = "APP_KEY";

export const storage = {
  async getBase() { return (await SecureStore.getItemAsync(BASE_URL_KEY)) ?? ""; },
  async setBase(v: string) { await SecureStore.setItemAsync(BASE_URL_KEY, v); },
  async getKey() { return (await SecureStore.getItemAsync(APP_KEY)) ?? ""; },
  async setKey(v: string) { await SecureStore.setItemAsync(APP_KEY, v); },
};
```

- [ ] **Step 2: Write `lib/api.ts`**

```typescript
import EventSource from "react-native-sse";
import { storage } from "./storage";

export type ToolCall = { name: string; args: any; id: string };

export async function chatStream(
  message: string,
  onText: (t: string) => void,
  onTools: (t: ToolCall[]) => void,
  onDone: () => void,
  onError: (e: any) => void,
) {
  const base = await storage.getBase();
  const key = await storage.getKey();
  const es = new EventSource(`${base}/chat`, {
    method: "POST",
    headers: { "x-app-key": key, "content-type": "application/json" },
    body: JSON.stringify({ message }),
    pollingInterval: 0,
  });
  es.addEventListener("text", (e: any) => onText(e.data));
  es.addEventListener("tool", (e: any) => onTools(JSON.parse(e.data)));
  es.addEventListener("done", () => { es.close(); onDone(); });
  es.addEventListener("error", (e: any) => { es.close(); onError(e); });
  return () => es.close();
}

export async function postJSON(path: string, body: any) {
  const base = await storage.getBase();
  const key = await storage.getKey();
  const r = await fetch(`${base}${path}`, {
    method: "POST",
    headers: { "x-app-key": key, "content-type": "application/json" },
    body: JSON.stringify(body),
  });
  if (!r.ok) throw new Error(`HTTP ${r.status}`);
  return r.json();
}

export async function getJSON(path: string) {
  const base = await storage.getBase();
  const key = await storage.getKey();
  const r = await fetch(`${base}${path}`, { headers: { "x-app-key": key } });
  if (!r.ok) throw new Error(`HTTP ${r.status}`);
  return r.json();
}

export async function deleteJSON(path: string) {
  const base = await storage.getBase();
  const key = await storage.getKey();
  const r = await fetch(`${base}${path}`, { method: "DELETE", headers: { "x-app-key": key } });
  if (!r.ok) throw new Error(`HTTP ${r.status}`);
  return r.json();
}
```

- [ ] **Step 3: Write `lib/theme.ts`**

```typescript
import React, { createContext, useContext, useState } from "react";

export type ThemeMode = "chaos" | "dark" | "calm" | "red_alert";

const PALETTES: Record<ThemeMode, { bg: string; fg: string; accent: string }> = {
  chaos:     { bg: "#0a0a0a", fg: "#ffffff", accent: "#ff2d55" },
  dark:      { bg: "#000000", fg: "#e5e5e5", accent: "#888888" },
  calm:      { bg: "#1a1a2e", fg: "#eaeaea", accent: "#7aa2f7" },
  red_alert: { bg: "#1a0000", fg: "#ffe5e5", accent: "#ff0000" },
};

const Ctx = createContext({ mode: "dark" as ThemeMode, set: (_:ThemeMode)=>{}, palette: PALETTES.dark });

export function ThemeProvider({ children }: any) {
  const [mode, setMode] = useState<ThemeMode>("dark");
  return <Ctx.Provider value={{ mode, set: setMode, palette: PALETTES[mode] }}>{children}</Ctx.Provider>;
}

export const useTheme = () => useContext(Ctx);
```

- [ ] **Step 4: Commit**

```bash
git add app/lib
git commit -m "feat(app): api client + theme + secure storage"
```

### Task 2.2: ChatScreen with streaming

**Files:**
- Create: `app/components/ChatBubble.tsx`
- Create: `app/(tabs)/chat.tsx`
- Modify: `app/_layout.tsx` (wrap với ThemeProvider)

- [ ] **Step 1: Write `components/ChatBubble.tsx`**

```typescript
import { Text, View } from "react-native";
import { useTheme } from "@/lib/theme";

export function ChatBubble({ role, content }: { role: "user"|"assistant"; content: string }) {
  const { palette } = useTheme();
  const isUser = role === "user";
  return (
    <View style={{
      alignSelf: isUser ? "flex-end" : "flex-start",
      backgroundColor: isUser ? palette.accent : "#222",
      padding: 10, borderRadius: 12, marginVertical: 4, maxWidth: "80%",
    }}>
      <Text style={{ color: palette.fg }}>{content}</Text>
    </View>
  );
}
```

- [ ] **Step 2: Write `(tabs)/chat.tsx`**

```typescript
import { useState, useRef } from "react";
import { View, TextInput, Pressable, Text, ScrollView, KeyboardAvoidingView, Platform } from "react-native";
import { chatStream } from "@/lib/api";
import { ChatBubble } from "@/components/ChatBubble";
import { useTheme } from "@/lib/theme";
import { executeTools } from "@/lib/toolExecutor";

type Msg = { role: "user"|"assistant"; content: string };

export default function Chat() {
  const { palette } = useTheme();
  const [msgs, setMsgs] = useState<Msg[]>([{role:"assistant", content:"Lâu không thấy. Lại có chuyện gì rồi à?"}]);
  const [input, setInput] = useState("");
  const [streaming, setStreaming] = useState(false);
  const scroll = useRef<ScrollView>(null);

  async function send() {
    if (!input.trim() || streaming) return;
    const user = input.trim();
    setInput("");
    setMsgs(m => [...m, {role:"user",content:user}, {role:"assistant",content:""}]);
    setStreaming(true);
    await chatStream(
      user,
      (t) => setMsgs(m => {
        const copy = [...m];
        copy[copy.length-1] = {role:"assistant", content: copy[copy.length-1].content + t};
        return copy;
      }),
      (tools) => executeTools(tools),
      () => setStreaming(false),
      (e) => { console.error(e); setStreaming(false); },
    );
  }

  return (
    <KeyboardAvoidingView behavior={Platform.OS==="ios"?"padding":undefined} style={{flex:1, backgroundColor: palette.bg}}>
      <ScrollView ref={scroll} onContentSizeChange={()=>scroll.current?.scrollToEnd({animated:true})} style={{padding: 12}}>
        {msgs.map((m,i)=><ChatBubble key={i} role={m.role} content={m.content}/>)}
      </ScrollView>
      <View style={{flexDirection:"row", padding: 8, gap: 8}}>
        <TextInput value={input} onChangeText={setInput} placeholder="nói đi..."
          placeholderTextColor="#888"
          style={{flex:1, color: palette.fg, backgroundColor:"#222", padding: 10, borderRadius: 8}}
          onSubmitEditing={send}/>
        <Pressable onPress={send} style={{backgroundColor: palette.accent, padding: 10, borderRadius: 8}}>
          <Text style={{color: palette.fg}}>gửi</Text>
        </Pressable>
      </View>
    </KeyboardAvoidingView>
  );
}
```

- [ ] **Step 3: Update `app/_layout.tsx`**

Wrap root với `<ThemeProvider>`. Verify Expo Router tabs structure includes chat as default.

- [ ] **Step 4: Smoke test trên Expo Go**

```bash
cd app && npx expo start
```
Trên Samsung mở Expo Go, scan QR. Vào Settings (sẽ làm sau, tạm hard-code base URL), gõ "tao buồn vl", verify stream chữ chạy đúng giọng.

- [ ] **Step 5: Commit**

```bash
git add app
git commit -m "feat(app): ChatScreen streaming"
```

### Task 2.3: Tool executor

**Files:**
- Create: `app/lib/toolExecutor.ts`

- [ ] **Step 1: Write `lib/toolExecutor.ts`**

```typescript
import * as Haptics from "expo-haptics";
import { Linking } from "react-native";
import type { ToolCall } from "./api";

// Global theme setter wired in _layout (escape hatch — simpler than full Redux for v1)
let setThemeMode: (m: any) => void = () => {};
export function bindThemeSetter(fn: (m:any)=>void) { setThemeMode = fn; }

export async function executeTools(tools: ToolCall[]) {
  for (const t of tools) {
    try {
      switch (t.name) {
        case "trigger_haptic": {
          const p = t.args.pattern;
          if (p === "heavy") await Haptics.impactAsync(Haptics.ImpactFeedbackStyle.Heavy);
          else if (p === "double") {
            await Haptics.impactAsync(Haptics.ImpactFeedbackStyle.Medium);
            setTimeout(() => Haptics.impactAsync(Haptics.ImpactFeedbackStyle.Medium), 120);
          } else {
            await Haptics.notificationAsync(Haptics.NotificationFeedbackType.Warning);
          }
          break;
        }
        case "change_theme": setThemeMode(t.args.mode); break;
        case "suggest_song": {
          const q = encodeURIComponent(t.args.query);
          Linking.openURL(`https://www.youtube.com/results?search_query=${q}`);
          break;
        }
        case "memory_save": /* server already extracts; client noop */ break;
      }
    } catch (e) { console.warn("tool error", t.name, e); }
  }
}
```

- [ ] **Step 2: Wire `bindThemeSetter` in `_layout.tsx`**

```typescript
// inside ThemeProvider consumer in root layout
import { useEffect } from "react";
import { bindThemeSetter } from "@/lib/toolExecutor";
import { useTheme } from "@/lib/theme";

function WireTools() {
  const { set } = useTheme();
  useEffect(() => { bindThemeSetter(set); }, [set]);
  return null;
}
// Render <WireTools/> inside provider tree.
```

- [ ] **Step 3: Test bằng cách prompt AI: "rung máy đi"**

Verify máy rung heavy. Try "đổi theme đỏ chaos" → background đổi sang chaos palette.

- [ ] **Step 4: Commit**

```bash
git add app/lib/toolExecutor.ts app/app/_layout.tsx
git commit -m "feat(app): tool executor (haptic/theme/song)"
```

### Task 2.4: MemoryStash screen

**Files:**
- Create: `app/components/MemoryCard.tsx`
- Create: `app/(tabs)/memory.tsx`

- [ ] **Step 1: Write `components/MemoryCard.tsx`**

```typescript
import { View, Text, Pressable } from "react-native";
import { useTheme } from "@/lib/theme";

export function MemoryCard({ item, onDelete }: { item: any; onDelete: ()=>void }) {
  const { palette } = useTheme();
  return (
    <View style={{flexDirection:"row", padding: 12, backgroundColor:"#1c1c1c",
                  marginVertical: 4, borderRadius: 8, alignItems:"center"}}>
      <View style={{flex:1}}>
        <Text style={{color: palette.fg, fontSize: 14}}>{item.content}</Text>
        <Text style={{color: "#888", fontSize: 11, marginTop: 4}}>
          {item.tag}{item.pinned ? " · 📌 seed" : ""}
        </Text>
      </View>
      {!item.pinned && (
        <Pressable onPress={onDelete} style={{padding: 8}}>
          <Text style={{color: palette.accent}}>🗑</Text>
        </Pressable>
      )}
    </View>
  );
}
```

- [ ] **Step 2: Write `(tabs)/memory.tsx`**

```typescript
import { useEffect, useState, useCallback } from "react";
import { View, Text, FlatList, RefreshControl } from "react-native";
import { getJSON, deleteJSON } from "@/lib/api";
import { MemoryCard } from "@/components/MemoryCard";
import { useTheme } from "@/lib/theme";

export default function Memory() {
  const { palette } = useTheme();
  const [items, setItems] = useState<any[]>([]);
  const [refreshing, setRefreshing] = useState(false);
  const load = useCallback(async () => {
    setRefreshing(true);
    try { setItems(await getJSON("/memory")); } catch {}
    setRefreshing(false);
  }, []);
  useEffect(() => { load(); }, [load]);
  return (
    <View style={{flex: 1, backgroundColor: palette.bg, padding: 12}}>
      <Text style={{color: palette.fg, fontSize: 18, marginBottom: 8}}>
        App đang nhớ về mày ({items.length})
      </Text>
      <FlatList
        data={items}
        keyExtractor={i => i.id}
        refreshControl={<RefreshControl refreshing={refreshing} onRefresh={load} tintColor={palette.fg}/>}
        renderItem={({item}) => <MemoryCard item={item} onDelete={async () => {
          await deleteJSON(`/memory/${item.id}`); load();
        }}/>}
      />
    </View>
  );
}
```

- [ ] **Step 3: Smoke test**

Vào tab Memory → thấy seed memories pinned + memories app tự lưu sau vài turn chat. Xóa 1 cái → verify backend `select count(*) from memories` giảm.

- [ ] **Step 4: Commit**

```bash
git add app/components/MemoryCard.tsx app/app/(tabs)/memory.tsx
git commit -m "feat(app): MemoryStash UI"
```

### Task 2.5: VibeSettings screen (base URL + key + theme picker + reset)

**Files:**
- Create: `app/(tabs)/settings.tsx`

- [ ] **Step 1: Write `(tabs)/settings.tsx`**

```typescript
import { useEffect, useState } from "react";
import { View, Text, TextInput, Pressable, Alert } from "react-native";
import { storage } from "@/lib/storage";
import { useTheme, ThemeMode } from "@/lib/theme";

const MODES: ThemeMode[] = ["chaos","dark","calm","red_alert"];

export default function Settings() {
  const { mode, set, palette } = useTheme();
  const [base, setBase] = useState("");
  const [key, setKey] = useState("");
  useEffect(() => { storage.getBase().then(setBase); storage.getKey().then(setKey); }, []);

  async function save() {
    await storage.setBase(base.trim());
    await storage.setKey(key.trim());
    Alert.alert("ok", "đã lưu");
  }

  return (
    <View style={{flex:1, backgroundColor: palette.bg, padding: 16, gap: 12}}>
      <Text style={{color: palette.fg, fontSize: 14}}>Backend URL</Text>
      <TextInput value={base} onChangeText={setBase} autoCapitalize="none"
        style={{color: palette.fg, backgroundColor:"#222", padding: 10, borderRadius: 8}}/>
      <Text style={{color: palette.fg, fontSize: 14}}>App Key</Text>
      <TextInput value={key} onChangeText={setKey} autoCapitalize="none" secureTextEntry
        style={{color: palette.fg, backgroundColor:"#222", padding: 10, borderRadius: 8}}/>
      <Pressable onPress={save} style={{backgroundColor: palette.accent, padding: 12, borderRadius: 8}}>
        <Text style={{color: palette.fg, textAlign:"center"}}>Lưu</Text>
      </Pressable>

      <Text style={{color: palette.fg, marginTop: 16}}>Theme</Text>
      <View style={{flexDirection:"row", flexWrap:"wrap", gap: 8}}>
        {MODES.map(m => (
          <Pressable key={m} onPress={() => set(m)}
            style={{padding: 8, borderRadius: 6,
              backgroundColor: m === mode ? palette.accent : "#222"}}>
            <Text style={{color: palette.fg}}>{m}</Text>
          </Pressable>
        ))}
      </View>
    </View>
  );
}
```

- [ ] **Step 2: Smoke test**

Set base URL = Render URL, set key = APP_SHARED_KEY env. Quay lại Chat → chat hoạt động.

- [ ] **Step 3: Commit + tag end of Day 2**

```bash
git add app/app/(tabs)/settings.tsx
git commit -m "feat(app): VibeSettings"
git tag day-2-complete
```

---

## Phase 3: Signature Features + Ship (Day 3)

### Task 3.1: Shake-to-Rant detection + VentingVoid screen

**Files:**
- Create: `app/lib/shake.ts`
- Create: `app/vent.tsx`

- [ ] **Step 1: Write `lib/shake.ts`**

```typescript
import { useEffect } from "react";
import { Accelerometer } from "expo-sensors";
import { router } from "expo-router";

export function useShakeNavigation() {
  useEffect(() => {
    Accelerometer.setUpdateInterval(100);
    let last = 0;
    const sub = Accelerometer.addListener(({x,y,z}) => {
      const force = Math.sqrt(x*x + y*y + z*z);
      const now = Date.now();
      if (force > 2.4 && now - last > 2500) {
        last = now;
        router.push("/vent");
      }
    });
    return () => sub.remove();
  }, []);
}
```

- [ ] **Step 2: Call `useShakeNavigation()` in tabs layout**

In `app/(tabs)/_layout.tsx`, add `useShakeNavigation()` at component top.

- [ ] **Step 3: Write `app/vent.tsx`**

```typescript
import { useState } from "react";
import { View, Text, TextInput, Pressable } from "react-native";
import { router } from "expo-router";
import * as Haptics from "expo-haptics";
import { postJSON } from "@/lib/api";

export default function Vent() {
  const [text, setText] = useState("");
  const [reply, setReply] = useState<string | null>(null);
  const [burning, setBurning] = useState(false);

  async function burn() {
    if (!text.trim()) return;
    await Haptics.impactAsync(Haptics.ImpactFeedbackStyle.Heavy);
    setBurning(true);
    try {
      const r = await postJSON("/vent", { text });
      setReply(r.reply);
      setText("");
      setTimeout(() => setBurning(false), 1800);
    } catch (e) { setBurning(false); }
  }

  return (
    <View style={{flex:1, backgroundColor:"#1a0000", padding: 24, justifyContent:"center"}}>
      <Text style={{color:"#fff", fontSize: 22, marginBottom: 12, fontWeight:"700"}}>
        🔥 Hố đen. Trút đi.
      </Text>
      <Text style={{color:"#ff9999", fontSize: 12, marginBottom: 16}}>
        Mọi thứ mày gõ ở đây KHÔNG được lưu. Đốt xong là xong.
      </Text>
      <TextInput
        value={text} onChangeText={setText} multiline placeholder="chửi đi..."
        placeholderTextColor="#aa6666"
        style={{minHeight: 160, color:"#fff", backgroundColor:"#330000",
                padding: 12, borderRadius: 8, textAlignVertical: "top"}}/>
      <Pressable onPress={burn} disabled={burning}
        style={{backgroundColor:"#ff0000", padding: 14, borderRadius: 8, marginTop: 16}}>
        <Text style={{color:"#fff", textAlign:"center", fontWeight:"700"}}>
          {burning ? "🔥 đang đốt..." : "ĐỐT"}
        </Text>
      </Pressable>
      {reply && (
        <View style={{marginTop: 24, padding: 12, backgroundColor:"#330000", borderRadius: 8}}>
          <Text style={{color:"#ffe5e5"}}>{reply}</Text>
        </View>
      )}
      <Pressable onPress={() => router.back()} style={{marginTop: 24, alignItems:"center"}}>
        <Text style={{color:"#aa6666"}}>← thoát</Text>
      </Pressable>
    </View>
  );
}
```

- [ ] **Step 4: Test lắc máy trên Samsung**

Lắc mạnh → navigate vào `/vent` → gõ text → ĐỐT → backend trả reply → màn không clear input giữa session.

- [ ] **Step 5: Commit**

```bash
git add app/lib/shake.ts app/app/vent.tsx app/app/(tabs)/_layout.tsx
git commit -m "feat(app): Shake-to-Rant + VentingVoid"
```

### Task 3.2: RageRoom

**Files:**
- Create: `app/components/Weapon.tsx`
- Create: `app/components/BurnAnim.tsx`
- Create: `app/(tabs)/rage.tsx`

- [ ] **Step 1: Write `components/Weapon.tsx`**

```typescript
import { Pressable, Text } from "react-native";

const EMOJI: Record<string,string> = { hammer:"🔨", bat:"🏏", grenade:"💣", fire:"🔥" };

export function Weapon({ id, selected, onPress }: { id: string; selected: boolean; onPress: ()=>void }) {
  return (
    <Pressable onPress={onPress} style={{padding: 16, borderRadius: 12,
        backgroundColor: selected ? "#ff2d55" : "#222", margin: 4}}>
      <Text style={{fontSize: 28}}>{EMOJI[id]}</Text>
    </Pressable>
  );
}
```

- [ ] **Step 2: Write `components/BurnAnim.tsx`**

```typescript
import { useEffect } from "react";
import Animated, { useSharedValue, withTiming, useAnimatedStyle, withSequence, runOnJS } from "react-native-reanimated";
import { Text } from "react-native";

export function BurnAnim({ text, onDone }: { text: string; onDone: ()=>void }) {
  const opacity = useSharedValue(1);
  const scale = useSharedValue(1);
  useEffect(() => {
    scale.value = withSequence(withTiming(1.2,{duration:120}), withTiming(0.3,{duration:600}));
    opacity.value = withTiming(0, { duration: 800 }, (done) => { if (done) runOnJS(onDone)(); });
  }, []);
  const style = useAnimatedStyle(() => ({ opacity: opacity.value, transform:[{scale:scale.value}] }));
  return <Animated.View style={style}><Text style={{color:"#fff", fontSize: 20}}>{text}</Text></Animated.View>;
}
```

- [ ] **Step 3: Write `(tabs)/rage.tsx`**

```typescript
import { useState } from "react";
import { View, Text, TextInput, Pressable } from "react-native";
import * as Haptics from "expo-haptics";
import { Weapon } from "@/components/Weapon";
import { BurnAnim } from "@/components/BurnAnim";
import { postJSON } from "@/lib/api";
import { useTheme } from "@/lib/theme";

const WEAPONS = ["hammer","bat","grenade","fire"];

export default function Rage() {
  const { palette } = useTheme();
  const [text, setText] = useState("");
  const [weapon, setWeapon] = useState("hammer");
  const [burning, setBurning] = useState<string | null>(null);
  const [quip, setQuip] = useState<string | null>(null);

  async function smash() {
    if (!text.trim()) return;
    const target = text;
    setText("");
    const r = await postJSON("/rage", { target_text: target, weapon });
    const pattern = r.haptic_pattern;
    if (pattern === "heavy") await Haptics.impactAsync(Haptics.ImpactFeedbackStyle.Heavy);
    else await Haptics.notificationAsync(Haptics.NotificationFeedbackType.Warning);
    setBurning(target);
    setQuip(r.quip);
  }

  return (
    <View style={{flex:1, backgroundColor: palette.bg, padding: 16}}>
      <Text style={{color: palette.fg, fontSize: 16, marginBottom: 8}}>
        Gõ thứ cần đập. Chọn vũ khí. Đập.
      </Text>
      <TextInput value={text} onChangeText={setText} placeholder="ký ức tệ, tên gì đó..."
        placeholderTextColor="#888"
        style={{color: palette.fg, backgroundColor:"#222", padding: 10, borderRadius: 8}}/>
      <View style={{flexDirection:"row", marginVertical: 12}}>
        {WEAPONS.map(w => <Weapon key={w} id={w} selected={w===weapon} onPress={()=>setWeapon(w)}/>)}
      </View>
      <Pressable onPress={smash} style={{backgroundColor: palette.accent, padding: 14, borderRadius: 8}}>
        <Text style={{color: palette.fg, textAlign:"center", fontWeight:"700"}}>ĐẬP</Text>
      </Pressable>
      <View style={{flex:1, justifyContent:"center", alignItems:"center"}}>
        {burning && <BurnAnim text={burning} onDone={()=>setBurning(null)}/>}
        {quip && !burning && <Text style={{color: palette.accent, fontSize: 16, marginTop: 16}}>{quip}</Text>}
      </View>
    </View>
  );
}
```

- [ ] **Step 4: Test trên Samsung**

Gõ "kỷ niệm Đà Lạt" → búa → ĐẬP → rung heavy + text bay biến mất.

- [ ] **Step 5: Commit**

```bash
git add app/components/Weapon.tsx app/components/BurnAnim.tsx app/app/(tabs)/rage.tsx
git commit -m "feat(app): RageRoom"
```

### Task 3.3: Push notifications (backend `/push/tick` + heuristic + Expo Push)

**Files:**
- Create: `backend/routes/push.py`
- Create: `backend/tests/test_push_heuristic.py`
- Create: `app/lib/notifications.ts`
- Modify: `backend/main.py`, `app/app/_layout.tsx`

- [ ] **Step 1: Write `tests/test_push_heuristic.py`**

```python
from routes.push import should_send_push
from datetime import datetime, timedelta

def test_block_outside_hours():
    now = datetime(2026,5,21,15,0)  # 15h
    assert should_send_push(now, last_push=None, sent_today=0) is False

def test_allow_late_night():
    now = datetime(2026,5,21,1,30)
    assert should_send_push(now, last_push=None, sent_today=0) is True

def test_block_recent_push():
    now = datetime(2026,5,21,1,30)
    last = now - timedelta(hours=2)
    assert should_send_push(now, last_push=last, sent_today=1) is False

def test_block_cap_reached():
    now = datetime(2026,5,21,11,30)
    assert should_send_push(now, last_push=None, sent_today=5) is False
```

- [ ] **Step 2: Write `routes/push.py`**

```python
import os, json, httpx
from datetime import datetime, timedelta
from fastapi import APIRouter, Request, HTTPException
from pydantic import BaseModel
from anthropic import AsyncAnthropic
from config import settings, load_seed, load_prompt
from db import get_pool

router = APIRouter()
_client = AsyncAnthropic(api_key=settings.anthropic_api_key)

def should_send_push(now: datetime, last_push: datetime|None, sent_today: int) -> bool:
    if sent_today >= 5: return False
    if last_push and (now - last_push) < timedelta(hours=4): return False
    h = now.hour
    if not (h >= 23 or h < 3 or (10 <= h < 12)): return False
    return True

class RegisterIn(BaseModel):
    token: str

@router.post("/push/register")
async def register_device(payload: RegisterIn, request: Request):
    if request.headers.get("x-app-key") != settings.app_shared_key:
        raise HTTPException(401)
    # store in settings table or env override; v1: write to file
    with open("/tmp/expo_push_token","w") as f: f.write(payload.token)
    return {"ok": True}

def _get_token() -> str | None:
    try:
        with open("/tmp/expo_push_token") as f: return f.read().strip() or None
    except FileNotFoundError:
        return None

@router.post("/push/tick")
async def push_tick(request: Request):
    if request.headers.get("x-cron-key") != settings.app_shared_key:
        raise HTTPException(401)
    now = datetime.utcnow() + timedelta(hours=7)  # ICT
    pool = await get_pool()
    async with pool.acquire() as conn:
        last_row = await conn.fetchrow("select sent_at from push_log order by sent_at desc nulls last limit 1")
        today_count = await conn.fetchval(
            "select count(*) from push_log where sent_at::date = $1", now.date()
        )
        last_mood = await conn.fetchval(
            "select mood from mood_snapshots order by created_at desc limit 1"
        ) or "unknown"
        mems = await conn.fetch("select content from memories order by created_at desc limit 3")
    last_push = last_row["sent_at"] if last_row else None
    if not should_send_push(now, last_push, today_count or 0):
        return {"skipped": True}

    seed = load_seed()
    prompt = load_prompt("push").format(
        nickname=seed["user"]["nickname"],
        hours_since=int((now - (last_push or now - timedelta(hours=12))).total_seconds()/3600),
        now=now.strftime("%H:%M"),
        last_mood=last_mood,
        top_memories="; ".join(m["content"] for m in mems),
    )
    resp = await _client.messages.create(
        model=settings.extract_model, max_tokens=120,
        messages=[{"role":"user","content":prompt}],
    )
    raw = resp.content[0].text
    try:
        data = json.loads(raw[raw.find("{"):raw.rfind("}")+1])
        msg = data.get("push")
    except Exception:
        msg = None
    if not msg:
        return {"skipped": True, "reason": "ai_said_no"}

    token = _get_token()
    if not token: return {"skipped": True, "reason":"no_token"}
    async with httpx.AsyncClient() as h:
        r = await h.post("https://exp.host/--/api/v2/push/send",
            json={"to": token, "body": msg, "sound": "default"})
    async with pool.acquire() as conn:
        await conn.execute("insert into push_log(message, sent_at) values($1,$2)", msg, now)
    return {"sent": True, "message": msg}
```

- [ ] **Step 3: Register in `main.py`**

```python
from routes import push as push_route
app.include_router(push_route.router)
```

- [ ] **Step 4: Write `app/lib/notifications.ts`**

```typescript
import * as Notifications from "expo-notifications";
import * as Device from "expo-device";
import { postJSON } from "./api";

export async function registerForPush() {
  if (!Device.isDevice) return;
  const { status } = await Notifications.requestPermissionsAsync();
  if (status !== "granted") return;
  const token = (await Notifications.getExpoPushTokenAsync()).data;
  try { await postJSON("/push/register", { token }); } catch {}
}
```

- [ ] **Step 5: Call `registerForPush()` in `_layout.tsx`**

```typescript
useEffect(() => { registerForPush(); }, []);
```

- [ ] **Step 6: Run heuristic tests**

```bash
pytest backend/tests/test_push_heuristic.py -v
```
Expected: 4 PASS.

- [ ] **Step 7: Set up cron-job.org**

- Login → New cronjob
- URL: `https://love-bot-api.onrender.com/push/tick`
- Method: POST
- Headers: `x-cron-key: <APP_SHARED_KEY>`
- Schedule: every hour at :00

- [ ] **Step 8: Manual test**

```bash
curl -X POST -H "x-cron-key: $APP_SHARED_KEY" https://love-bot-api.onrender.com/push/tick
```
Expected: `{"sent":true,"message":"..."}` nếu giờ phù hợp, else `{"skipped":true}`.

- [ ] **Step 9: Commit**

```bash
git add backend/routes/push.py backend/tests/test_push_heuristic.py backend/main.py app/lib/notifications.ts app/app/_layout.tsx
git commit -m "feat: push notifications cron + heuristic"
```

### Task 3.4: Branding, intro, EAS build

**Files:**
- Modify: `app/app.json`
- Create: `app/assets/icon.png`, `splash.png` (đơn giản)
- Modify: `app/(tabs)/chat.tsx` (intro logic)

- [ ] **Step 1: Update `app.json`**

```json
{
  "expo": {
    "name": "Mèo & Tao",
    "slug": "love-bot",
    "scheme": "lovebot",
    "version": "1.0.0",
    "platforms": ["android"],
    "android": {
      "package": "com.dev.lovebot",
      "permissions": ["VIBRATE", "RECEIVE_BOOT_COMPLETED"]
    },
    "plugins": ["expo-router","expo-haptics","expo-sensors","expo-notifications","expo-secure-store"],
    "icon": "./assets/icon.png",
    "splash": { "image": "./assets/splash.png", "backgroundColor": "#0a0a0a" }
  }
}
```

- [ ] **Step 2: Generate simple icon**

Tạo file PNG đơn giản 1024x1024 — chữ "M" hoặc emoji 🐱 trên nền đen, accent đỏ. Có thể dùng Canva/Figma.

- [ ] **Step 3: Intro message khi mở app lần đầu**

In `(tabs)/chat.tsx`, replace cứng câu chào bằng:
```typescript
const FIRST_RUN_KEY = "FIRST_RUN_DONE";
useEffect(() => {
  (async () => {
    const done = await SecureStore.getItemAsync(FIRST_RUN_KEY);
    if (!done) {
      setMsgs([{role:"assistant", content:"Ờ. Thằng dev nó làm cái này cho mày. Đừng cảm động vội — tao là loại khó tính. Bắt đầu kể đi."}]);
      await SecureStore.setItemAsync(FIRST_RUN_KEY, "1");
    }
  })();
}, []);
```

- [ ] **Step 4: EAS configure**

```bash
cd app
npx eas-cli login
npx eas-cli build:configure
```

Edit `eas.json`:
```json
{
  "build": {
    "preview": {
      "android": { "buildType": "apk" },
      "distribution": "internal"
    }
  }
}
```

- [ ] **Step 5: Build APK**

```bash
npx eas-cli build --profile preview --platform android
```

Đợi ~10-15 phút trên cloud. Lấy URL `.apk`.

- [ ] **Step 6: Cài lên Samsung của bạn, smoke test full flow**

- Mở app → câu chào intro
- Chat vài câu → voice đúng, AI nhớ seed
- Vào Memory → thấy seed pinned + memories tự lưu
- Lắc máy → vào Vent → đốt → reply
- Vào Rage → đập text → rung heavy + animation
- Chỉnh theme → đổi màu
- Đợi push trong khung giờ → nhận notification

- [ ] **Step 7: Fix bugs đúc kết từ smoke test**

(Placeholder cho việc fix tại chỗ — chạy lại `eas build` sau khi fix.)

- [ ] **Step 8: Gửi APK + lời ngỏ**

Gửi qua Zalo/Telegram. Kèm hướng dẫn: "cài đè, bật quyền cài app ngoài Play Store, vào Settings điền base URL + key".

- [ ] **Step 9: Final commit + tag**

```bash
git add app
git commit -m "feat: branding + EAS build config"
git tag v1.0.0-mvp
```

---

## Self-Review

**Spec coverage:**
- Architecture (§1) → Phase 0 + 1 ✓
- Seed + Style (§2) → Task 0.1 step 5 ✓
- Screens (§3.1) → Tasks 2.2, 2.4, 2.5, 3.1, 3.2 ✓
- Endpoints (§3.2) → Tasks 1.5, 1.6, 1.7, 3.3 ✓
- Tools (§3.3) → Task 2.3 ✓
- Push logic (§3.4) → Task 3.3 ✓
- Data model (§4.1) → Task 1.1 ✓
- Memory flow (§4.2, §4.3) → Tasks 1.4, 1.5 ✓
- Privacy boundary (§4.4) → Task 1.6 ✓
- Seed bootstrap (§4.5) → Task 1.8 ✓
- Deploy (§5) → Tasks 1.9, 3.4 ✓
- Risk register → handled inline (cap push, reinject style — TODO post-MVP) ✓

**Placeholder scan:** Task 3.4 step 7 có placeholder "fix bugs từ smoke test" — đây là expected vì không biết trước bug nào, nhưng acceptable cho 1 plan ship cá nhân.

**Type consistency:** `chat.tsx` uses `executeTools` (matches `toolExecutor.ts` export). `postJSON/getJSON/deleteJSON` consistent. `should_send_push` signature consistent giữa test và impl.

---

## Execution Handoff

Plan đã lưu tại `docs/superpowers/plans/2026-05-21-love-bot.md`. Hai lựa chọn execute:

**1. Subagent-Driven (recommended)** — Tôi dispatch subagent mới cho mỗi task, review giữa các task, iterate nhanh.

**2. Inline Execution** — Tôi execute tasks trong session này theo executing-plans, batch với checkpoint cho bạn review.

Bạn chọn cách nào?
